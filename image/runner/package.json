{
  "name": "@pi-dispatch/runner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The SDK job runner. Runs inside the job container; never on the host.",
  "license": "MIT",
  "main": "run-job.mjs",
  "engines": {
    "node": ">=22.19.0"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "0.80.7",
    "@playwright/cli": "0.1.17"
  }
}
